---
# linode ansible playbook
- name: GPT-OSS with Open Webui setup
  hosts: localhost
  connection: local
  any_errors_fatal: true
  user: root
  vars_files: [group_vars/linode/vars]
  roles:
    - common
    - gpt-oss
    - post
