---
- name: Add addons
  import_role:
    name: addons
  when: add_ons != ['none']

- name: Ensure controller and nginx are started and enabled
  systemd_service:
    name: "{{ item }}"
    state: started
    enabled: true
  loop: [nginx, controller]

- name: Copy MOTD template to /etc/motd
  template:
    src: templates/motd.j2
    dest: /etc/motd
    mode: '0600'
    owner: '{{ username }}'
    group: '{{ username }}'

- name: Writing creds into file
  copy:
    dest: '/home/{{ username }}/.credentials'
    mode: '0600'
    owner: '{{ username }}'
    group: '{{ username }}'
    content: |
      Sudo Username: {{ username }}
      Sudo Password: {{ password }}

      HaltDOS Management UI: https://{{ _domain }}:{{ haltdos_ext_port }}/setup
      NGINX Setup-Wizard Auth Username: {{ haltdos_htpasswd_user }}
      NGINX Setup-Wizard Auth Password: {{ haltdos_htpasswd_password }}

      First-run steps:
        1. Open the Management UI above and complete the wizard.
        2. After your first listener is configured, start the WAF data plane:
           sudo systemctl start offloader
