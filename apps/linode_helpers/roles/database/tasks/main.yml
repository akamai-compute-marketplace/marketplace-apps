# marketplace sql database helper
---
- name: Install MySQL
  include_tasks:
    file: mysql.yml
  when: sql_db == 'mysql'

- name: Install MariaDB
  include_tasks:
    file: mariadb.yml
  when: sql_db == 'mariadb'
