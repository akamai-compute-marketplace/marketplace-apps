---
# linode ansible playbook

- name: Gemma3 with Open Webui setup
  hosts: localhost
  connection: local
  any_errors_fatal: true
  user: root
  vars_files: [group_vars/linode/vars]
  roles:
    - common
    - gemma3
    - post
