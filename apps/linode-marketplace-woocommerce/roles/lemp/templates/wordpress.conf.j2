# /etc/nginx/snippets/wordpress.conf
location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
    expires max;
    log_not_found off;
}

location = /xmlrpc.php {
    deny all;
    access_log off;
    log_not_found off;
    return 444;
}
