---
- name: Allow UFW SSH access
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  loop: ["22"]

- name: Enable UFW
  community.general.ufw:
    state: enabled
    policy: deny
