[Unit]
Description=My Node.js App
After=network.target

[Service]
ExecStart=/usr/bin/node /var/www/{{ _domain }}/app.js
WorkingDirectory=/var/www/{{ _domain }}
Restart=always
User={{ username }}
Group={{ username }}
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target