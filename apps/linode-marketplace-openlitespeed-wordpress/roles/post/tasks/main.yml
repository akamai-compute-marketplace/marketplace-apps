-
# motd and creds gen
- name: copy MOTD template to /etc/motd
  template:
    src: templates/motd.j2
    dest: /etc/motd

# - name: writing creds into file
#   copy:
#     dest: /home/{{ username }}/.credentials
#     mode: '0600'
#     content: |
#       Web Admin password: 
#         {{ litespeed_password }}
#       MySQL root password and MySQL Wordpress user password:
#         {{ db_password }}
#       Sudo Username: {{ username }}
#       Sudo Password: {{ password }}

- name: writing creds into file
  copy:
    dest: /home/{{ username }}/.credentials
    mode: '0600'
    content: |
      Web Admin password: 
        {{ litespeed_password }}
      MySQL root password: {{ root_mysql_pass }}
      MySQL Wordpress user password: {{ wordpress_mysql_pass }}
      Sudo Username: {{ username }}
      Sudo Password: {{ password }}

- name: Remove /tmp/lshttpd
  ansible.builtin.file:
    path: /tmp/lshttpd
    state: absent